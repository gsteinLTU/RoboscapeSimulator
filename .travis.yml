language: node_js
node_js:
    - lts/*

sudo: required
services:
    - docker

before_install:
    - docker build -f Dockerfile -t gstein17/roboscape-simulator .

script:
    - docker run -e CI=true gstein17/roboscape-simulator sh -c "npm install -g jest && npm run test --coverage"

after_success:
    - if [[ "$TRAVIS_BRANCH" == "master" ]]; then
      docker login -u gstein17 -p $DOCKER_HUB_KEY ;
      docker push gstein17/roboscape-simulator ;
      fi

deploy:
    provider: elasticbeanstalk
    region: 'us-east-1'
    app: 'RoboScape Simulator'
    env: 'RoboscapeSimulator-env'
    bucket_name: 'elasticbeanstalk-us-east-1-631166995015'
    bucket_path: 'RoboscapeSimulator'
    access_key_id: $AWS_ACCESS_KEY
    secret_access_key: $AWS_SECRET_KEY
    on:
        branch: master
